import numpy
import numpy as np

np

x=np.zeros(10) # this will create an array called zeros with zeros
y=np.ones(10) # this will create an array called y with ones
print(x)
print(type(x)) # here all the data types are in float, because the default data type of the array is float,
print(x.dtype)
print(y)
print(type(y))
print(y.dtype)

z=np.ones(10,dtype=int)
print(z)
print(type(z))
print(z.dtype)

