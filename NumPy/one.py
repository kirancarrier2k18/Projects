import numpy as np
#default data type of the array is float
x=np.array([1,2,3,4,5,6]) # if we have another data type ex: strings, the rest of the date types will get changed to strings...
print(x)
print(type(x))
print(x.dtype)

